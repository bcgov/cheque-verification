PORT=4000
API_URL=http://localhost:3000
FRONTEND_URL=http://localhost:5173
NODE_ENV=development

# Optional JWT settings for upstream API authentication.
# Replace the placeholder values with environment-specific secrets.
# JWT_SECRET=replace-with-shared-secret
# JWT_ISSUER=backend service
# JWT_AUDIENCE=api service
# JWT_TTL=120
